target/
/data/
*.sqlite*
# Build/distribution artifacts (produced by packaging/bundle.sh)
/dist/
# Generated iconset scratch
*.iconset/

# Extension signing private key — never commit
packaging/extension-key/key.pem

# OTA update token (fine-grained read-only PAT baked in at build) — never commit
packaging/.update-token
